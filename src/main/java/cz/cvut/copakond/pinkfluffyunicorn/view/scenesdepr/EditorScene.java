package cz.cvut.copakond.pinkfluffyunicorn.view.scenesdepr;

import javafx.scene.canvas.GraphicsContext;
import javafx.scene.paint.Color;
import javafx.scene.text.Text;
import javafx.stage.Stage;
/*
public class EditorScene extends ResponsiveScene {

    public EditorScene(Stage stage) {
        super(stage);
        Text editorText = new Text("Level Editor");
        editorText.setFill(Color.WHITE);
    }

    protected void onResizeCanvas(double width, double height) {
        double fontSize = height / 25;
    }

    @Override
    protected void draw() {
        // Example simple drawing for testing purposes
        GraphicsContext gc = canvas.getGraphicsContext2D();
        gc.setFill(Color.BLACK);
        gc.fillRect(0, 0, canvas.getWidth(), canvas.getHeight()); // Fill canvas with black

        gc.setFill(Color.WHITE);
        gc.fillText("Welcome to Pink Fluffy Unicorn!", canvas.getWidth() / 2 - 100, canvas.getHeight() / 2); // Add some text to center
    }

}*/